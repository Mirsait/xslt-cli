(ns mirsait.xslt-cli-test
  (:require [clojure.test :refer [deftest is testing]]
            [mirsait.xslt-cli :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))
